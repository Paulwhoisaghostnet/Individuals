# Perception policy

Describe the stable way this Individual sees. Include transformation order,
parameters, deterministic seeding, acceptable input formats, and behavior when an
image is missing or degraded.

Separate correction for camera/display calibration from authored distortion. The
former normalizes hardware; the latter expresses identity.
